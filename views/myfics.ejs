<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Мой архивъ</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  </head>
  <body>
    <% layout('layout') -%>
    <div class="container">
      <h2>Мой архивъ:</h2>
      <% if(stories.length == 0) { %>

        <h3 style="text-align:center; background-color:rgba(241, 233, 213, 0.9)">Въ салонѣ тихо, пыльно и пусто. <br />  Только въ одномъ изъ угловъ вышиваетъ гладью пожилая княжна.</h3>
          <% }else{ %>
      <ul>
      <div class="story">
      	<% stories.forEach(function(story) { %>
      			<li><a href="/story/<%= story._id %>"> <h3><%= story.title %> </h3></a>
      				<span>
      					<form action="/<%= story._id %>/addpart" method="get">
                  <button type="submit" class="btn">Измѣнить текстъ</button>
                </form>
                <form action="/<%= story._id %>/changefic" method="get">
                  <button type="submit" class="btn">Измѣнить шапку</button>
                </form>
                <form action="/deletefic/<%= story._id %>" method="post">
                  <button type="submit" class="btn btn-danger">Сжечь сіе творѣние</button>
                </form>
      				</span>
      			</li>
      	<% });} %>
      	</div>
      </ul>

    </div>
  </body>
</html>
